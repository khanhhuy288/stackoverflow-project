[project]
name = "stackoverflow-project"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "autogluon-tabular[all]>=1.5.0",
    "ipykernel>=7.2.0",
    # Ranges compatible with AutoGluon Tabular 1.5.x (full `autogluon` also needs numpy<2.2 via timeseries).
    "numpy>=1.26,<2.2",
    "pandas>=2.0,<2.3",
    "pyspark>=4.1.1",
    "setuptools>=75.0.0,<82",
    "ydata-profiling>=4.18.1",
]
